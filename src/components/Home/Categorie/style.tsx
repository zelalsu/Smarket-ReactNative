import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    marginTop: 20,
    borderRadius: 8,
    borderWidth: 1,
  },
  innerContainer: {
    flexDirection: 'row',
    width: 187,
    height: 70,
  },
});
export default style;
